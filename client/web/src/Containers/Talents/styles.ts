import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(
  theme => ({
    box:{
      marginTop:10,
    },
    content:{
      display: 'flex',
      justifyContent: 'flex-end',
      marginBottom: 10,
      marginTop: 4,
    }
  }),
);
