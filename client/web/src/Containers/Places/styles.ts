import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(
  theme => ({
    box:{
      marginTop:14,
    },
    content:{
      display: 'flex',
      justifyContent: 'flex-end',
      marginBottom: 10,
      marginTop: 4,
    }
  }),
);
