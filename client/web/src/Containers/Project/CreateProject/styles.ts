import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(
  theme => ({
    mainPage: { },
    mainContainer: {
      paddingLeft: 30,
      paddingRight: 30,
    },
  }),
  { name: "CreateProject" }
);
